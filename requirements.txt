from pathlib import Path
p=Path("/mnt/data/requirements.txt")
text=p.read_text()
# Existing dependencies are sufficient for the backend described; add explicit multipart only if needed? No.
out=Path("/mnt/data/requirements_updated.txt")
out.write_text("""fastapi>=0.109.0
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.0
""")
print(out)
